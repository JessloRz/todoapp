# azure-pipelines.yml
pool:
  vmImage: ubuntu-latest

trigger:
  - main

resources:
  repositories:
    - repository: 'templetes'
      type: git
      name: templates-project/templetes  # Proyecto/Repositorio
      ref: refs/heads/main
    - repository: 'articulo'
      type: git
      name: DevOpsEjemplo/articulo     # Proyecto/Repositorio
      ref: refs/heads/main

extends:
  template: springboot-ci-template.yml@templetes
  parameters:
    repoName: 'articulo'
    mavenGoals: 'clean package'
    environment: 'dev'
